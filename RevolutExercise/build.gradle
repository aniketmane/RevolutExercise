// Top-level build file where you can add configuration options common to all sub-projects/modules.
buildscript {
    ext.kotlin_version = "1.3.72"
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:4.0.0"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

ext {
    // Sdk and tools
    minSdkVersion = 24
    targetSdkVersion = 29
    compileSdkVersion = 29
    buildToolsVersion = '29.0.3'
    constraintLayoutVersion = '1.1.3'
    recyclerViewVersion = '1.0.0'
    cardViewVersion = '1.0.0'

    // App dependencies
    supportLibraryVersion = '1.0.2'
    materialVersion = '1.1.0'
    vectorDrawableVersion = '1.0.1'
    animatedVectorDrawableVersion = '1.0.0'
    gsonVersion = '2.8.2'
    aapCompat = '1.1.0'
    recyclerViewVersion = '1.1.0'
    fragmentKotlinExtn = '1.3.0-alpha06'
    fragmentExtn = '1.2.5'
    couroutine_ext = '1.3.5'

    //Kotlin
    kotlinExt = '1.3.0'

    //Dagger
    dagger2Version = '2.16'

    //Retrofit
    retrofit = '2.8.1'
    retrofitConverter = '2.8.1'
    retrofit_call_adapter = '1.0.0'

    //OkHTTP
    okhttp3_version = '4.6.0'

    //Moshi
    moshi_version = '1.9.2'

    //Glide
    glide_version = '4.11.0'


    //lifeCycle
    lifecycleVersion = '2.2.0'
    lifecycleExtensionVersion = '1.1.1'

    // Test dependencies
    junitVersion = '4.13'
    junitExtVersion = '1.1.1'
    espressoVersion = '3.2.0'
    mockitoVersion = '3.3.3'
    archExtn = '2.1.0'

    //Room
    room_version = '2.3.0-alpha01'
}

task clean(type: Delete) {
    delete rootProject.buildDir
}